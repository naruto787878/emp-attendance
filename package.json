{
  "name": "emp-management",
  "version": "1.0.0",
  "description": "Employee Management System - MERN Stack with SQLite",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && npm run dev",
    "client": "cd frontend && npm start",
    "install-all": "cd backend && npm install && cd ../frontend && npm install",
    "setup": "npm run install-all && echo \"✓ All packages installed — run npm run dev to start\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
